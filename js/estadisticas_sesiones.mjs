import{dbLoadExercises}from"./database.mjs";function crearTarjetaSesion(e){const t=document.createElement("a");t.className="card",t.href=`progreso_volumen.html?sesion=${encodeURIComponent(e)}`,t.setAttribute("aria-label",`Ver progreso de volumen de la sesión ${e}`);const n=document.createElement("div");return n.className="card-name",n.textContent=`Sesión ${e}`,t.appendChild(n),t}function mostrarVacio(e,t){const n=document.createElement("div");n.className="empty-state",n.textContent=t,e.replaceWith(n)}async function cargarSesiones(){const e=await dbLoadExercises(),t=new Set;for(const n of e)if(Array.isArray(n.sesion))for(const e of n.sesion)t.add(e);return[...t].sort((e,t)=>e-t)}!async function(){const e=document.getElementById("contenedorSesiones");try{const t=await cargarSesiones();if(!t.length)return void mostrarVacio(e,"Todavía no hay sesiones registradas.");document.getElementById("subtituloPagina").textContent=`${t.length} sesiones`;const n=document.createDocumentFragment();for(const e of t)n.appendChild(crearTarjetaSesion(e));e.appendChild(n)}catch(t){console.error("[estadisticas_sesiones]",t),mostrarVacio(e,"No se pudieron cargar las sesiones.")}}();